name: native_animated_image_example
description: "Example app demonstrating native_animated_image (Rust-based GIF/APNG/WebP decoder for Flutter)."
publish_to: 'none'
version: 0.1.0

resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  native_animated_image:
    path: ../packages/native_animated_image
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
